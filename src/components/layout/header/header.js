import React from 'react';

const header = (props) =>{
    return(
        <header>
            <button>add blog</button>
            <button>add project</button>
        </header>
    );
}

export default header;